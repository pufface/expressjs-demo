<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>User Index</title>
  </head>
  <body>
    <h1>Handlebars</h1>
    {{#each users as |user|}}
    <div>
        <img src="/profilepic/{{user.username}}_sm.png" alt="">
        <a href="/{{user.username}}">{{user.name.first}} {{user.name.last}}</a>
    </div>
    {{/each}}
  </body>
</html>
